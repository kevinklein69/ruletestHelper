# ruletestHelper
Chrome extension for ClassMarker.com that automatically learns correct quiz answers from the results page and auto-fills them on future runs. Supports single-choice and multiple-choice questions, with export/import to share answer sets between users.
